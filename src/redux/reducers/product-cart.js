const initialState = {
  products: [],
  isFetching: false,
  error: {
    status: false,
    message: ''
  }
};

const productCartReducer = (state = initialState, action) => {

};

export default productCartReducer;
